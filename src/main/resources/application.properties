spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.mvc.pathmatch.matching-strategy=ant-path-matcher

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PASS:5432}/${DB_SCHEMA:finder}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:gzv970}

server.port=${SERVER_PORT:8080}

com.korom.filefinder.rootpath=${ROOT_PATH:C:/Users/korom/Documents/test}
com.korom.filefinder.username=${USERNAME:korom}